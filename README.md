# weather-vpn-app

Weather lookup app (city / zip / GPS coordinates), backed by OpenWeatherMap.
Deployed inside a private Docker network that's only reachable over WireGuard.

More docs and setup instructions to follow as the project comes together.
